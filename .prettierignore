# Managed by the HelmIt workflow commands, never by a formatter.
# Formatting these breaks the KEEL hard rule that `.helmit/` is only edited
# through the workflow commands.
.helmit/

# Project documents written by hand: prose formatting is not the formatter's call.
*.md

# Generated by drizzle-kit and rewritten on every `npm run db:generate`.
# Reformatting them would only make the next generation dirty the diff again.
drizzle/

node_modules/
dist/
coverage/
dev-data/

# Design system and screen prototype: working input for the interface phase,
# formatted by the tool that produced it. What lands in `src/web/` is formatted
# by this project's rules.
mychat-design-system/
